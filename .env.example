# Base URL of your (self-hosted, containerized) Rust dashboard API.
# Read in the browser by src/lib/api.ts. Must be prefixed PUBLIC_ to be
# exposed client-side. Leave pointing at localhost during local dev.
PUBLIC_API_BASE=http://localhost:8080
